struct People {
  pub name;
  pub age;
  pub weight;
  pub height;
  pub desc?;
}

fn test_use_struct_init {
  var john = People {
    name = "John",
    age = 21,
    weight = 70,
    height = 178,
  }
}

trait Walkable {
  stand();
  walk(distance);
  startRacing(self);
  gooseStep(self, type);
}
