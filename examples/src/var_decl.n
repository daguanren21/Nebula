fn generate_random_four_nums {
  // ... Todo: implement returning an array with four random numbers
}

fn test_var_decl {
  const bit_num = 0x8F32E;
  var a = 1, b = 5.3, c = false, d;
  var [first, second] = generate_random_four_nums();
  const [a,b, ...rest] = [1,2,3,4,5,6];
}